{
  "buildCommand": "npx prisma generate && pnpm expo export -p web",
  "outputDirectory": "dist",
  "devCommand": "pnpm expo start --web",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
